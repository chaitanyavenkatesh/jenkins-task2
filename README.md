# Jenkins task 2
## Objective
configure a Jenkins SSH Agent and connect it to the Jenkins Master

## completed Tasks
- Installed and configured Jenkins.
- Configured OpenSSH server.
- Created Jenkins agents(agent1).
- Configured SSH credentials.
- Connected Jenkins Master to the agent.
- Verified the agent status is online.

